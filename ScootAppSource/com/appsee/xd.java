package com.appsee;

import android.view.MotionEvent;
import java.util.HashMap;

class xd
{
  private x A;
  short B = 1500;
  private HashMap<Integer, jd> G = new HashMap();
  int l = 30;
  
  public xd(x paramx)
  {
    this.A = paramx;
  }
  
  public boolean C(MotionEvent paramMotionEvent)
  {
    int i = paramMotionEvent.getActionIndex();
    int j = paramMotionEvent.getPointerId(i);
    switch (paramMotionEvent.getActionMasked())
    {
    }
    for (;;)
    {
      return false;
      C(j, paramMotionEvent.getX(i), paramMotionEvent.getY(i), paramMotionEvent.getEventTime());
      continue;
      C(j, paramMotionEvent.getX(i), paramMotionEvent.getY(i), paramMotionEvent.getEventTime());
      continue;
      C(j, paramMotionEvent.getX(i), paramMotionEvent.getY(i), paramMotionEvent.getEventTime(), paramMotionEvent);
      continue;
      C(j, paramMotionEvent.getX(i), paramMotionEvent.getY(i), paramMotionEvent.getEventTime(), paramMotionEvent);
    }
  }
}


/* Location:              D:\Android\dex2jar-2.0\classes-dex2jar.jar!\com\appsee\xd.class
 * Java compiler version: 6 (50.0)
 * JD-Core Version:       0.7.1
 */